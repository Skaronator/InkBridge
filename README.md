# InkBridge

A lightweight App to capture Home-Assistant dashboard screenshots and optimize them for eInk displays.

The addon implementation now uses Go, Chromium DevTools (via `chromedp`), and a built-in dithering engine compatible with the existing color schemes and dither modes.
